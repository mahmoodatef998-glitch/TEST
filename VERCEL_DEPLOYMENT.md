# دليل نشر المشروع على Vercel

## 📋 المتطلبات الأساسية

1. حساب على Vercel (مجاني)
2. حساب GitHub (المشروع موجود بالفعل على GitHub)
3. Node.js مثبت محلياً (للاختبار)

---

## 🚀 الطريقة الأولى: النشر من GitHub (الأسهل)

### الخطوة 1: تسجيل الدخول إلى Vercel

1. اذهب إلى [vercel.com](https://vercel.com)
2. اضغط على **Sign Up** أو **Log In**
3. اختر **Continue with GitHub** لتسجيل الدخول بحساب GitHub

### الخطوة 2: استيراد المشروع

1. بعد تسجيل الدخول، اضغط على **Add New Project**
2. ستجد قائمة بمشاريعك على GitHub
3. ابحث عن **TEST** أو **mahmoodatef998-glitch/TEST**
4. اضغط على **Import** بجانب المشروع

### الخطوة 3: إعداد المشروع

في صفحة الإعدادات:

1. **Project Name**: اتركه كما هو أو غيره (مثلاً: `perkins-cummins-generators`)
2. **Framework Preset**: اختر **Next.js** (سيتم اكتشافه تلقائياً)
3. **Root Directory**: اتركه فارغاً أو ضع `frontend` إذا أردت نشر الواجهة الأمامية فقط
4. **Build Command**: اتركه تلقائياً (سيستخدم `npm run build`)
5. **Output Directory**: اتركه تلقائياً (`.next`)

### الخطوة 4: إعداد متغيرات البيئة (Environment Variables)

إذا كان لديك متغيرات بيئة:

1. اضغط على **Environment Variables**
2. أضف المتغيرات المطلوبة (مثل `API_URL`)

### الخطوة 5: النشر

1. اضغط على **Deploy**
2. انتظر حتى يكتمل البناء (Build)
3. بعد اكتمال البناء، ستحصل على رابط للموقع (مثل: `your-project.vercel.app`)

---

## 🔧 الطريقة الثانية: النشر باستخدام Vercel CLI

### الخطوة 1: تثبيت Vercel CLI

```bash
npm install -g vercel
```

### الخطوة 2: تسجيل الدخول

```bash
vercel login
```

### الخطوة 3: الانتقال لمجلد الواجهة الأمامية

```bash
cd frontend
```

### الخطوة 4: النشر

```bash
vercel
```

اتبع التعليمات:
- **Set up and deploy?** → اضغط `Y`
- **Which scope?** → اختر حسابك
- **Link to existing project?** → اضغط `N` للمشروع الأول
- **Project name?** → اتركه أو غيره
- **Directory?** → اضغط Enter (استخدم `.`)

### الخطوة 5: النشر للإنتاج

```bash
vercel --prod
```

---

## ⚙️ إعداد ملف vercel.json (اختياري)

أنشئ ملف `vercel.json` في مجلد `frontend`:

```json
{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://your-backend-url.com/api/:path*"
    }
  ]
}
```

---

## 🔄 نشر الواجهة الخلفية (Backend)

Vercel يدعم Serverless Functions. يمكنك تحويل الـ backend إلى Vercel Functions:

### الطريقة 1: استخدام Vercel Serverless Functions

1. أنشئ مجلد `api` داخل `frontend`
2. انقل ملفات الـ backend إلى `frontend/api`
3. Vercel سيتعرف عليها تلقائياً

### الطريقة 2: نشر Backend على منصة منفصلة

يمكنك نشر الـ backend على:
- **Railway** (railway.app)
- **Render** (render.com)
- **Heroku** (heroku.com)
- **DigitalOcean App Platform**

ثم حدّث رابط الـ API في الواجهة الأمامية.

---

## 📝 تحديث رابط API في الواجهة الأمامية

بعد نشر الـ backend، حدّث رابط API في `frontend/components/Contact.tsx`:

```typescript
const response = await fetch('https://your-backend-url.com/api/contact', {
  // ...
})
```

أو استخدم متغير بيئة:

```typescript
const API_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:5000'
const response = await fetch(`${API_URL}/api/contact`, {
  // ...
})
```

ثم أضف في Vercel:
- **Environment Variable**: `NEXT_PUBLIC_API_URL`
- **Value**: رابط الـ backend الخاص بك

---

## 🎯 خطوات سريعة (Quick Steps)

### للنشر السريع:

1. ✅ سجل دخول إلى [vercel.com](https://vercel.com) بحساب GitHub
2. ✅ اضغط **Add New Project**
3. ✅ اختر مشروع **TEST** من GitHub
4. ✅ اضغط **Import**
5. ✅ في **Root Directory** ضع: `frontend`
6. ✅ اضغط **Deploy**
7. ✅ انتظر حتى يكتمل البناء
8. ✅ استمتع بموقعك المباشر!

---

## 🔍 التحقق من النشر

بعد النشر:

1. ستحصل على رابط مثل: `https://your-project.vercel.app`
2. افتح الرابط في المتصفح
3. تحقق من أن الموقع يعمل بشكل صحيح

---

## 📌 ملاحظات مهمة

1. **الواجهة الأمامية فقط**: Vercel مثالي لـ Next.js
2. **الواجهة الخلفية**: قد تحتاج إلى نشرها على منصة منفصلة
3. **المتغيرات البيئية**: أضفها في إعدادات المشروع على Vercel
4. **التحديثات التلقائية**: كل push إلى GitHub سيحدث الموقع تلقائياً

---

## 🆘 حل المشاكل الشائعة

### مشكلة: Build فشل
- تحقق من أن جميع التبعيات مثبتة في `package.json`
- تأكد من أن `build` script موجود

### مشكلة: API لا يعمل
- تحقق من رابط الـ API
- تأكد من إضافة متغيرات البيئة
- تحقق من CORS في الـ backend

### مشكلة: الصور لا تظهر
- تأكد من إضافة domain الصور في `next.config.js`

---

## 📚 روابط مفيدة

- [Vercel Documentation](https://vercel.com/docs)
- [Next.js Deployment](https://nextjs.org/docs/deployment)
- [Vercel Dashboard](https://vercel.com/dashboard)

---

## ✅ Checklist قبل النشر

- [ ] المشروع موجود على GitHub
- [ ] جميع التبعيات في `package.json`
- [ ] ملف `.gitignore` موجود
- [ ] `build` script يعمل محلياً
- [ ] رابط API محدث (إذا كان الـ backend منفصل)

---

**بالتوفيق في النشر! 🚀**

