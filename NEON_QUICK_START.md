# 🚀 دليل سريع: Neon Database (5 دقائق)

## الخطوات السريعة

### 1️⃣ إنشاء قاعدة بيانات على Neon

1. اذهب إلى: **https://neon.tech**
2. اضغط **Sign Up**
3. سجل دخول بحساب GitHub
4. اضغط **Create Project**
5. املأ:
   - **Name**: `perkins-cummins-db`
   - **Region**: اختر أقرب منطقة
   - **Compute**: Free
6. اضغط **Create Project**
7. انتظر 1-2 دقيقة

---

### 2️⃣ الحصول على Connection String

1. في Dashboard، ستجد **Connection String**
2. اضغط **Copy** لنسخه
3. سيبدو مثل:
   ```
   postgresql://user:pass@ep-xxxxx.region.aws.neon.tech/neondb?sslmode=require
   ```

---

### 3️⃣ إنشاء الجدول

1. اضغط **SQL Editor** في Neon
2. انسخ محتوى ملف `NEON_SQL.sql`
3. الصق في SQL Editor
4. اضغط **Run**

---

### 4️⃣ إضافة متغيرات البيئة

**في Railway/Render (Backend):**
1. اذهب إلى **Variables**
2. أضف:
   - `DATABASE_URL` = Connection String من Neon

**في Vercel (Frontend):**
- فقط `NEXT_PUBLIC_API_URL` = رابط الـ backend

---

### 5️⃣ تثبيت Neon

```bash
cd backend
npm install @neondatabase/serverless
```

---

### 6️⃣ اختبار

1. افتح موقعك
2. املأ نموذج الاتصال
3. في Neon → **SQL Editor**:
   ```sql
   SELECT * FROM contact_messages;
   ```
4. ✅ يجب أن ترى الرسالة!

---

## ✅ Checklist

- [ ] حساب Neon جاهز
- [ ] مشروع تم إنشاؤه
- [ ] Connection String تم نسخه
- [ ] جدول `contact_messages` تم إنشاؤه
- [ ] متغيرات البيئة تم إضافتها
- [ ] Neon تم تثبيته
- [ ] تم الاختبار بنجاح

---

## 🎯 النتيجة

الآن جميع رسائل نموذج الاتصال سيتم حفظها في قاعدة بيانات Neon!

---

**بالتوفيق! 🚀**

