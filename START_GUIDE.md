# دليل استخدام ملف START

## 🚀 التشغيل السريع

### على Windows:
1. انقر نقراً مزدوجاً على ملف **START.bat**
2. أو افتح Command Prompt واكتب:
   ```bash
   START.bat
   ```

### على PowerShell:
```powershell
.\START.ps1
```

### على Linux/Mac:
```bash
chmod +x START.sh
./START.sh
```

## ✨ ما يفعله ملف START:

1. **يشغل الواجهة الخلفية** في نافذة منفصلة
2. **يشغل الواجهة الأمامية** في نافذة منفصلة
3. **يفتح المتصفح تلقائياً** على http://localhost:3000

## 📋 ملاحظات مهمة:

- ✅ السيرفرات ستعمل في نوافذ منفصلة
- ✅ يمكنك إغلاق نافذة START بعد بدء التشغيل
- ✅ لإيقاف السيرفرات، أغلق نوافذ Command Prompt/PowerShell
- ✅ تأكد من أن المنفذين 3000 و 5000 غير مستخدمين

## 🔧 في حالة وجود مشاكل:

### إذا لم يفتح المتصفح تلقائياً:
افتح المتصفح يدوياً واذهب إلى: http://localhost:3000

### إذا ظهرت رسالة خطأ:
1. تأكد من تثبيت جميع التبعيات:
   ```bash
   npm run install:all
   ```
2. تأكد من أن Node.js مثبت (الإصدار 18 أو أحدث)
3. تأكد من أن المنفذين 3000 و 5000 غير مستخدمين

## 🌐 الروابط:

- **الواجهة الأمامية**: http://localhost:3000
- **الواجهة الخلفية**: http://localhost:5000
- **API Health Check**: http://localhost:5000/api/health

