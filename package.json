{
  "name": "perkins-cummins-generators",
  "version": "1.0.0",
  "description": "Professional website for PERKINS + CUMMINS generators",
  "scripts": {
    "start": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run open:browser\"",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "open:browser": "wait-on http://localhost:3000 && open-cli http://localhost:3000",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "open-cli": "^7.2.0",
    "wait-on": "^7.2.0"
  }
}
